import 'package:flutter/material.dart';

class SupplyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('SupplyScreen'),
    ));
  }
}
