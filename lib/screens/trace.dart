import 'package:flutter/material.dart';

class TraceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('TraceScreen'),
    ));
  }
}
